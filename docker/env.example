# PulseKit Docker Environment Variables

# REQUIRED: Generate a secret key base using: mix phx.gen.secret
SECRET_KEY_BASE=your-secret-key-base-here

# The hostname for your PulseKit instance
PHX_HOST=localhost

# Port to expose (default: 4000)
PORT=4000

